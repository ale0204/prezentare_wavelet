# Node
node_modules/
dist/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Generated images: commit .png, never .jpg
*.jpg
*.jpeg

# Every build writes its litter into a _build/ next to what it builds, so this one line
# covers the LaTeX aux files and the reports from the mechanical checks. Deliverables stay
# where they belong and are committed: docs/suport/suport.pdf, frontend/dist/.
_build/

# OS / editor
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Anything host-specific: accounts, keys, deploy parameters
.env
*.pem
*.key
